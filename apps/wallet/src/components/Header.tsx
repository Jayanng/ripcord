import { NetworkBadge } from './NetworkBadge';
import type { AppTab } from './TabBar';
export function Header({ active, onChange }: { active: AppTab; onChange: (tab: AppTab) => void }) { const tabs: AppTab[] = ['balance','send','receive','activity','ripcord']; return <header className="topbar"><button className="brand brand-button" aria-label="Ripcord home" onClick={()=>onChange('balance')}><span className="brand-mark" aria-hidden="true" /><span>RIPCORD</span></button><nav className="desktop-nav" aria-label="Primary navigation">{tabs.map(tab=><button key={tab} className={active===tab?'active':''} aria-current={active===tab?'page':undefined} onClick={()=>onChange(tab)}>{tab[0].toUpperCase()+tab.slice(1)}</button>)}</nav><NetworkBadge /></header>; }
